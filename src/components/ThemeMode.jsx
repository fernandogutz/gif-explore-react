import React from 'react'

const ThemeMode = () => {
    const toggleThemeMode = () => {
        
    }

    return (
        <button onClick={toggleThemeMode} id="themeMode">Change Theme Mode</button>
    )
}

export default ThemeMode